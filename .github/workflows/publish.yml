name: Publish Library

on:
  push:
    branches:
      - main  # main 브랜치에 push 시 자동 배포 (SNAPSHOT 버전)
    tags:
      - 'v*'  # v1.0.0, v1.1.0 등 태그 푸시 시 배포 (릴리즈 버전)
  workflow_dispatch:  # 수동 실행 가능
    inputs:
      version:
        description: 'Version to publish (e.g., 1.0.0)'
        required: true

env:
  # Organization: passion-edu
  # Repository: s-class-common
  GITHUB_PACKAGES_URL: https://maven.pkg.github.com/passion-edu/s-class-common

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Check GitHub App configuration
        id: check-app
        run: |
          if [ -z "${{ vars.APP_ID }}" ]; then
            echo "❌ APP_ID variable is NOT set"
            echo "Please set up GitHub App (see docs/SETUP_QUICK_START.md)"
            exit 1
          fi
          if [ -z "${{ secrets.APP_PRIVATE_KEY }}" ]; then
            echo "❌ APP_PRIVATE_KEY secret is NOT set"
            echo "Please set up GitHub App (see docs/SETUP_QUICK_START.md)"
            exit 1
          fi
          echo "✅ GitHub App configuration found"
          echo "APP_ID=${{ vars.APP_ID }}" >> $GITHUB_OUTPUT

      - name: Generate GitHub App Token
        id: generate-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: passion-edu
          repositories: s-class-common
      
      - name: Diagnose GitHub App permissions
        if: always()
        run: |
          TOKEN="${{ steps.generate-token.outputs.token }}"
          APP_ID="${{ vars.APP_ID }}"
          
          echo "🔍 GitHub App 진단 시작..."
          echo "App ID: $APP_ID"
          echo ""
          
          # 설치 정보 확인
          echo "Checking GitHub App installation..."
          INSTALLATION_RESPONSE=$(curl -s \
            -H "Authorization: Bearer $TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/app/installations" || echo "[]")
          
          if echo "$INSTALLATION_RESPONSE" | grep -q "id"; then
            echo "✅ GitHub App is installed"
            
            # 설치된 레포지토리 확인
            echo ""
            echo "Checking installed repositories..."
            REPOS_RESPONSE=$(curl -s \
              -H "Authorization: Bearer $TOKEN" \
              -H "Accept: application/vnd.github.v3+json" \
              "https://api.github.com/installation/repositories" || echo "{}")
            
            if echo "$REPOS_RESPONSE" | grep -q "s-class-common"; then
              echo "✅ s-class-common repository is accessible"
            else
              echo "⚠️ s-class-common repository might not be in the installation"
              echo "Response: $REPOS_RESPONSE"
            fi
          else
            echo "⚠️ GitHub App installation 정보를 가져올 수 없습니다"
            echo "   이는 정상일 수 있습니다 (installation access token 제한)"
          fi
          
          echo ""
          echo "📋 수동 확인 체크리스트 (⚠️ 반드시 확인하세요!):"
          echo ""
          echo "1. GitHub App의 Repository permissions 확인:"
          echo "   → https://github.com/organizations/passion-edu/settings/apps"
          echo "   → 's-class-package-publisher' 선택 → 'Edit' 클릭"
          echo "   → 'Repository permissions' 섹션으로 스크롤"
          echo "   → 'Packages' 드롭다운 확인 → 'Read and write'로 설정되어 있어야 함 ✅"
          echo "   → ⚠️ 이것이 가장 흔한 원인입니다!"
          echo ""
          echo "2. GitHub App 설치 확인:"
          echo "   → GitHub App 설정 페이지 → 'Install App' 클릭"
          echo "   → 's-class-common' 레포지토리가 설치 목록에 있는지 확인"
          echo ""
          echo "3. 권한 변경 후 재설치 (⚠️ 필수!):"
          echo "   → 권한을 변경했다면 반드시 재설치 필요!"
          echo "   → 기존 설치 제거 (Uninstall) → 재설치 (Install)"
          echo ""
          echo "📖 상세 가이드: docs/SETUP_QUICK_START.md 참고"

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          # GitHub Packages 인증 설정 (GitHub App 토큰 사용)
          # Organization 패키지의 경우 Organization 이름을 username으로 사용
          server-id: github
          server-username: passion-edu
          server-password: ${{ steps.generate-token.outputs.token }}
          settings-path: ${{ github.workspace }}/.m2
      
      - name: Create Gradle properties for authentication
        run: |
          mkdir -p ~/.gradle
          # Organization 패키지의 경우 Organization 이름을 username으로 사용
          echo "gpr.user=passion-edu" >> ~/.gradle/gradle.properties
          echo "gpr.token=${{ steps.generate-token.outputs.token }}" >> ~/.gradle/gradle.properties
      
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      
      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', 'gradle/**') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      
      - name: Lint Check
        run: ./gradlew ktlintCheck
      
      - name: Build and Test
        run: ./gradlew clean build test
      
      - name: Extract version from tag
        if: startsWith(github.ref, 'refs/tags/')
        id: tag
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
      
      - name: Extract version from build.gradle.kts (for branch push)
        if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
        id: version_from_file
        run: |
          VERSION=$(grep -oP 'version = "\K[^"]+' build.gradle.kts)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Update version in build.gradle.kts
        if: github.event_name == 'workflow_dispatch'
        run: |
          VERSION="${{ github.event.inputs.version }}"
          sed -i.bak "s/version = \".*\"/version = \"$VERSION\"/" build.gradle.kts
          rm -f build.gradle.kts.bak
      
      - name: Extract version from build.gradle.kts
        if: github.event_name == 'workflow_dispatch'
        id: version
        run: |
          VERSION=$(grep -oP 'version = "\K[^"]+' build.gradle.kts)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Verify GitHub App token
        run: |
          TOKEN="${{ steps.generate-token.outputs.token }}"
          if [ -z "$TOKEN" ]; then
            echo "❌ GitHub App token generation failed!"
            echo "Please check:"
            echo "  1. APP_ID variable is set correctly"
            echo "  2. APP_PRIVATE_KEY secret is set correctly"
            echo "  3. GitHub App is installed on the repository"
            exit 1
          fi
          echo "✅ GitHub App token generated successfully"
          echo "Token length: $(echo -n "$TOKEN" | wc -c)"
          echo "Repository: ${{ env.GITHUB_PACKAGES_URL }}"
          echo "User: ${{ github.actor }}"
      
      - name: Verify token is installation access token
        run: |
          TOKEN="${{ steps.generate-token.outputs.token }}"
          echo "Verifying token type..."
          echo ""
          echo "ℹ️ Note: GitHub App installation access token은 /user 엔드포인트를 사용할 수 없습니다 (정상 동작)"
          echo "   대신 레포지토리 접근으로 토큰 유효성을 확인합니다."
          echo ""
          
          # 레포지토리 접근 확인
          echo "Checking repository access..."
          REPO_RESPONSE=$(curl -s \
            -H "Authorization: Bearer $TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/passion-edu/s-class-common" || echo "{}")
          
          if echo "$REPO_RESPONSE" | grep -q "full_name"; then
            echo "✅ Token is valid installation access token"
            echo "✅ Can access repository: passion-edu/s-class-common"
          else
            echo "❌ Cannot access repository"
            echo "This might indicate:"
            echo "  1. GitHub App is not installed on the repository"
            echo "  2. Token generation failed"
            echo "Response: $REPO_RESPONSE"
            exit 1
          fi
          
          # Packages 권한 확인
          echo ""
          echo "Checking packages access via API..."
          PACKAGES_RESPONSE=$(curl -s \
            -H "Authorization: Bearer $TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/orgs/passion-edu/packages?package_type=maven" || echo "{}")
          
          if echo "$PACKAGES_RESPONSE" | grep -q "name"; then
            echo "✅ Can access organization packages via API"
          elif echo "$PACKAGES_RESPONSE" | grep -q "403\|401"; then
            echo "⚠️ Cannot access organization packages via API (403/401)"
            echo "   This might indicate missing Packages permission in Repository permissions"
            echo "   However, this check is not definitive - the actual test is in the next step"
            echo "   Response: $PACKAGES_RESPONSE"
          else
            echo "ℹ️ Cannot access organization packages via API or no packages exist yet"
            echo "   This is OK - the actual authentication test is in the next step"
            echo "   Response: $PACKAGES_RESPONSE"
          fi
      
      - name: Test GitHub Packages access
        run: |
          TOKEN="${{ steps.generate-token.outputs.token }}"
          
          # 토큰 확인
          if [ -z "$TOKEN" ]; then
            echo "❌ ERROR: GitHub App token is empty!"
            echo "This should not happen if GitHub App is configured correctly."
            exit 1
          fi
          
          echo "✅ Using GitHub App installation access token"
          echo "Token length: $(echo -n "$TOKEN" | wc -c)"
          echo "Token prefix: $(echo -n "$TOKEN" | cut -c1-10)..."
          
          URL="${{ env.GITHUB_PACKAGES_URL }}/com/s-class/common-kotlin-lib/maven-metadata.xml"
          echo ""
          echo "Testing access to: $URL"
          echo ""
          
          # GitHub App installation access token을 사용할 때는 특별한 형식이 필요할 수 있습니다
          # GitHub Packages Basic Auth는 username에 특정 값을 요구할 수 있습니다
          
          # 방법 1: Organization 이름 사용
          echo "🔍 Method 1: Using organization name as username"
          echo "Trying with organization name: passion-edu"
          HTTP_CODE1=$(curl -s -o /dev/null -w "%{http_code}" \
            -u "passion-edu:$TOKEN" \
            "$URL" 2>&1 || echo "000")
          echo "HTTP Status Code (with org name): $HTTP_CODE1"
          
          # 방법 2: GitHub App의 경우 username을 특별한 형식으로 시도
          # GitHub App installation access token은 때때로 username을 특정 형식으로 요구합니다
          echo ""
          echo "🔍 Method 2: Using x-access-token format (if supported)"
          HTTP_CODE2=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: token $TOKEN" \
            "$URL" 2>&1 || echo "000")
          echo "HTTP Status Code (with token header): $HTTP_CODE2"
          
          # 방법 3: Bearer token 형식
          echo ""
          echo "🔍 Method 3: Using Bearer token format"
          HTTP_CODE3=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer $TOKEN" \
            "$URL" 2>&1 || echo "000")
          echo "HTTP Status Code (with Bearer): $HTTP_CODE3"
          
          # 방법 4: 사용자명 사용
          if [ "$HTTP_CODE1" != "200" ] && [ "$HTTP_CODE1" != "404" ]; then
            echo ""
            echo "🔍 Method 4: Using actor username"
            echo "Trying with username: ${{ github.actor }}"
            HTTP_CODE4=$(curl -s -o /dev/null -w "%{http_code}" \
              -u "${{ github.actor }}:$TOKEN" \
              "$URL" 2>&1 || echo "000")
            echo "HTTP Status Code (with username): $HTTP_CODE4"
            HTTP_CODE="$HTTP_CODE4"
          else
            HTTP_CODE="$HTTP_CODE1"
          fi
          
          # 가장 성공한 방법 사용
          if [ "$HTTP_CODE1" = "200" ] || [ "$HTTP_CODE1" = "404" ]; then
            HTTP_CODE="$HTTP_CODE1"
          elif [ "$HTTP_CODE2" = "200" ] || [ "$HTTP_CODE2" = "404" ]; then
            HTTP_CODE="$HTTP_CODE2"
          elif [ "$HTTP_CODE3" = "200" ] || [ "$HTTP_CODE3" = "404" ]; then
            HTTP_CODE="$HTTP_CODE3"
          fi
          
          if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "404" ]; then
            echo ""
            echo "✅ Authentication successful (404 is OK for new packages)"
          elif [ "$HTTP_CODE" = "401" ]; then
            echo ""
            echo "❌ Authentication failed (401 = Unauthorized)"
            echo ""
            echo "⚠️ This is the actual problem - not the /user endpoint 403 (which is normal)"
            echo ""
            echo "🔍 상세 진단:"
            echo ""
            echo "가능한 원인:"
            echo "1. GitHub App의 Repository permissions에 Packages 권한이 없음 ⚠️ 가장 흔한 원인"
            echo "2. 권한을 변경했지만 GitHub App을 재설치하지 않음"
            echo "3. GitHub App이 레포지토리에 설치되지 않음"
            echo "4. Organization의 패키지 설정 문제"
            echo ""
            echo "🔧 해결 방법:"
            echo ""
            echo "1. GitHub App permissions 확인 (⚠️ 가장 중요!):"
            echo "   - Go to: https://github.com/organizations/passion-edu/settings/apps"
            echo "   - Find 's-class-package-publisher' GitHub App"
            echo "   - Click 'Edit' to modify permissions"
            echo "   - Scroll to 'Repository permissions' section"
            echo "   - Check 'Packages' dropdown → Must be 'Read and write' ✅ **필수**"
            echo "   - ⚠️ Repository permissions에 Packages 권한이 없으면 GitHub Packages에 접근할 수 없습니다!"
            echo "   - ⚠️ Note: Organization permissions에 Packages가 없을 수 있습니다 (정상)"
            echo "   - Click 'Update GitHub App' at the bottom"
            echo ""
            echo "2. GitHub App 재설치 (⚠️ 권한 변경 후 필수!):"
            echo "   - GitHub App 설정 페이지에서 'Install App' 클릭"
            echo "   - 기존 설치가 있다면:"
            echo "     → 설치 목록에서 선택"
            echo "     → 'Uninstall' 클릭"
            echo "   - 'Install App' 버튼 클릭"
            echo "   - 'Select repositories'에서 's-class-common' 선택 (또는 All repositories)"
            echo "   - 'Install' 클릭"
            echo "   - ⚠️ 권한을 변경한 경우 반드시 재설치해야 합니다!"
            echo ""
            echo "3. Organization package permissions 확인:"
            echo "   - Go to: https://github.com/organizations/passion-edu/settings/packages"
            echo "   - Ensure package creation is allowed for organization members"
            echo ""
            echo "4. GitHub App 설치 확인:"
            echo "   - GitHub App 설정 페이지 → 'Install App'"
            echo "   - 's-class-common' 레포지토리가 설치 목록에 있는지 확인"
            echo ""
            echo "📖 상세 가이드: docs/SETUP_QUICK_START.md 참고"
            exit 1
          else
            echo "⚠️ Unexpected status code: $HTTP_CODE"
          fi
      
      - name: Publish to GitHub Packages
        run: |
          echo "Publishing with GitHub App token:"
          echo "  URL: ${{ env.GITHUB_PACKAGES_URL }}"
          echo "  User: passion-edu (organization name)"
          echo ""
          ./gradlew publish \
            -Pgithub.packages.url=${{ env.GITHUB_PACKAGES_URL }} \
            -Pgpr.user=passion-edu \
            -Pgpr.token=${{ steps.generate-token.outputs.token }}
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
          GITHUB_ACTOR: passion-edu
      
      - name: Output published version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "Published version: ${{ steps.version.outputs.VERSION }}"
          elif startsWith(github.ref, 'refs/tags/'); then
            echo "Published version: ${{ steps.tag.outputs.VERSION }}"
          else
            echo "Published version: ${{ steps.version_from_file.outputs.VERSION }}"
          fi
      
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          name: Release ${{ steps.tag.outputs.VERSION }}
          body: |
            ## Changes
            See [CHANGELOG.md](CHANGELOG.md) for details.
            
            ## Installation
            ```kotlin
            dependencies {
                implementation("com.s-class:common-kotlin-lib:${{ steps.tag.outputs.VERSION }}")
            }
            ```
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
